apiVersion: triggers.tekton.dev/v1alpha1
kind: EventListener
metadata:
  name: checkride-shuma-f
spec:
  serviceAccountName: tekton-triggers-sa
  triggers:
  - name: express-sample-app-deploy
    interceptors:
    - github:
        eventTypes: ["push"]
    - cel:
        filter: "body.repository.name == 'checkride-shuma-frontend' && body.ref.split('/')[2] == 'main'"
    bindings:
    - ref: checkride-shuma-f-triggerbinding
    template:
      ref: checkride-shuma-f-triggertemplate