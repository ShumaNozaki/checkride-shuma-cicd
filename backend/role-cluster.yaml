apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: checkride-shuma-b-cl-role
  namespace: checkride-shuma-b
rules:
  - apiGroups:
      - triggers.tekton.dev
    resources:
      - triggerbindings
      - clusterinterceptors
    verbs:
      - get
      - list
      - watch
---

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: checkride-shuma-b-cl-rolebinding
  namespace: checkride-shuma-b
subjects:
  - kind: ServiceAccount
    name: tekton-triggers-sa
    namespace: checkride-shuma-b
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: checkride-shuma-b-cl-role